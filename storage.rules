rules_version = '2';

// Open rules — see firestore.rules. Restrict before public production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
