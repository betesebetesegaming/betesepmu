rules_version = '2';

// Open rules — the app uses custom phone/password auth in Firestore, not Firebase Auth,
// so role-based rules block normal client access. Tighten these before public production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
